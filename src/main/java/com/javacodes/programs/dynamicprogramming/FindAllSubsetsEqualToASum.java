package com.javacodes.programs.dynamicprogramming;

/**
 * Problem: Find all subsets whose sum is equal to 'S'
 * Note:
 * Use the solution for find all subsets of a set.
 * Have conditions to terminate the branch which doesn't satisfy the condition.
 * condition: so far weight included <= 'S'
 * and Condition: so far weight included + remaining weight >=30
 */
public class FindAllSubsetsEqualToASum {

    public static void main(String args[]){

    }
}
